# The Simplified Charts - Full Clean Baseline

This archive is a complete project baseline. Keep the folder structure exactly as-is when uploading to GitHub.

Important:
- `src/app/page.tsx` is the PUBLIC HOMEPAGE.
- `src/app/admin/page.tsx` is the ADMIN PAGE and must remain separate.
- Do not copy `admin/page.tsx` into `src/app/page.tsx`.
- Existing Supabase data is not stored in this ZIP; database data remains in Supabase.
